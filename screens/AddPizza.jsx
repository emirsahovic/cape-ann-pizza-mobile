import { StyleSheet, Text, View } from "react-native";

const AddPizza = () => {
  return (
    <View>
      <Text>AddPizza</Text>
    </View>
  );
};

export default AddPizza;

const styles = StyleSheet.create({});
